<!-- Minimal home template for Shopify theme -->
<main role="main">
  <section class="hero">
    <div class="wrap">
      <h1>{{ shop.name }}</h1>
      <p>{{ shop.description }}</p>
    </div>
  </section>

  <section class="content">
    <div class="wrap">
      <h2>Bienvenido</h2>
      <p>Este tema incluye un scaffold mínimo. Reemplaza este contenido con tus secciones.</p>
    </div>
  </section>
</main>
